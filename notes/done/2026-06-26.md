---
status: draft
title: Done 2026-06-26
date: 2026-06-26
---

# Done — 2026-06-26

## Что сделано

- Фон-система `/dev/canvas-bg`: `.ds-canvas` — прозрачные секции (surface = только тон, без заливки) + единый «lobes»-градиент за группой (анимированный: дрейф пятен + пульс яркости/насыщенности, лавандовый центр, viewport-fixed). Перенесён из gallery-light градиента.
- v2 Hero (`StatementHero`) — стеклянная плашка без встроенной картинки: фрост преломляет фон/градиент за секцией.
- Плавающие GSAP-блобы (`CanvasBlobs.tsx`): автономный idle-дрейф + scroll-параллакс (преимущественно вниз); раскладка hero по референсу `balls-glass.png` + по одному peeker-блобу на экран + пара за карточками Horizontal Gallery; `.ds-blob{max-width:none}`; 4 PNG-ассета в `public/assets/backgrounds/blobs/`.
- Демо-страница `/dev/canvas-bg` (hero=v2, dark-футер). Всё закоммичено и запушено в `main`.

## Что сделано (продолжение сессии)

### /product/entry-hub — дизайн-ритм завершён

- **Zone-rhythm dark→light→dark** по паттерну `/trust/prompt-injection-safety`:
  - §1–3 ink: тёмная база `ds-zone__bg--on` (lobes-dark видна сразу)
  - §3→§4: `ZoneToneFlipReverse` — переход ink→light с brand bloom
  - §4–5 light: светлый остров + `ZoneBlobs top="38%" bottom="28%"`
  - §5→§6: `ZoneToneFlip targetSelector=".ds-redark"` — возврат light→ink
  - §6–7 ink: второй тёмный слой `.ds-redark`
- `CanvasFlowField blue` — синие flow-blobs поверх тёмных секций
- `tr-gradient-bridge` 200px (transparent→ink) перед CtaBand
- CtaBand `theme="dark" bleed videoSrc="/assets/cta/neo.mp4"`
- Footer `variant="dark"`
- §7 EditorialSplit v3, 3 пункта
- §6 Gallery `titleAccent="You decide"` (расширен LabGallery новыми пропами `titleLead`/`titleAccent`)

**Осталось по странице:**
- [ ] Загрузить изображения (media-слоты — сейчас placeholders)
- [ ] Вычитать и откорректировать тексты

**Паттерн dark→light→dark** задокументирован в памяти и в `wiki/product/entry-hub.md` (раздел «Статус реализации»). Эталон для будущих страниц — `/trust/prompt-injection-safety/page.tsx`.

## Что в процессе

- Парный тёмный градиент для ink-зон (дорожная карта «два градиента light/dark»).
- Тюнинг позиций блобов под стеклянными карточками (галерея / HubMap) — выверять в реальном браузере.
- Клип `ZoneBlobs top/bottom` на entry-hub — подогнать вживую через `?blobs` инспектор.

## Что заблокировано / открытые вопросы

- Нет блокеров. Грабли инструмента: на dev-стендах headless-preview скролл сбрасывается в 0, GSAP/CSS-анимации на паузе в скрытой вкладке → визуальную проверку движения и середины страницы делать в активном браузере.

## Ссылки

- Linear:
- PR / коммиты: `fc898a5` (.ds-canvas), `dc79691` (canvas-bg), `239663a` (entry-hub zone-rhythm) → main
- wiki: `wiki/product/entry-hub.md` — раздел «Статус реализации»
- notes/daily:
