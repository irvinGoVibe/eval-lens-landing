---
status: draft
title: Done 2026-07-04
date: 2026-07-04
---

# Done — 2026-07-04

## Что сделано

- **Глубокий аудит производительности** (read-only, 4 параллельных агента): медиа-веса, used/unused CSS, JS-бандл, рантайм анимаций. Отчёт в чате + основа для Блока 4 ниже.
- **Контрольный замер (baseline)** перед оптимизацией: прод-сборка `b19009e` в изолированном worktree, Chrome DevTools MCP (mobile, Slow 4G, CPU×4). Зафиксирован в `.claude/runs/optimization/perf-baseline-2026-07-04.md`.
- **7 коммитов perf-оптимизации** на ветке `optimization` (`22e3c30`…`8d0bd71`):
  - immutable cache-control для `/assets/*` + AVIF в `next/image`
  - bitrate-cap re-encode 17 фоновых видео (SSIM-гейт ≥0.94), −31.5 MB
  - `ScrollOrchestrator` — честный teardown (закрыта утечка слушателей/observers/rAF после SPA-навигации), IO-гейт deck-физики, throttled header-sync, read/write-фазы scrub-тика
  - 19 постеров для фоновых видео (24 call site), 4 тяжёлых PNG → webp, 2 webp пережаты
  - gsap/BlobInspector переведены на ленивую загрузку; `ogl`/`@gsap/react` удалены
  - мёртвый CSS-блок `.egr-*` + микро-правила удалены (−121 строка)
  - `Cinema` переведён на `LazyVideo` (было: `<video autoPlay>` на 8 below-fold секциях)
- **Итог (тот же метод до/после)**: вес `/` 10.33→5.21 MB (−50%), `/trust/methodology` 4.49→0.84 MB (−81%), `/pricing` 1.99→0.85 MB (−57%), `public/` 112.6→76.7 MB (−32%). LCP главной на Slow 4G упирается в hero-хореографию (`maxWait`), не в вес медиа — **user решил хореографию не трогать (финально)**.
- Верификация: `tsc --noEmit` чисто, прод-сборка (63 страницы) зелёная, Playwright smoke 8 passed/1 skipped/0 failed.
- Полный отчёт: [wiki/reports/2026-07-04-optimization-audit.md](../../wiki/reports/2026-07-04-optimization-audit.md) (Блок 4).

## Что в процессе

- Ничего не оставлено в незавершённом состоянии — все 7 коммитов прошли верификацию.

## Что заблокировано / открытые вопросы

- **Визуальный прогон глазами** — headless-preview не видит анимации (известная грабля); нужно пролистать `/`, `/trust/methodology`, `/pricing` в живом дев-сервере user (скраб `#problem`, zone-флипы, постеры видео).
- **`NEXT_PUBLIC_SITE_URL` в Vercel Production** — не выставлена, sitemap/OG возьмут авто-домен без неё.
- Merge `optimization` → `main` — после визуального ОК user.

## Ссылки

- Linear:
- PR / коммиты: `22e3c30`, `658b11a`, `d824aec`, `fd9ba74`, `9f227c6`, `c3fb180`, `8d0bd71` (ветка `optimization`)
- wiki: [reports/2026-07-04-optimization-audit.md](../../wiki/reports/2026-07-04-optimization-audit.md)
- notes/daily:
- Контрольные замеры: `.claude/runs/optimization/perf-baseline-2026-07-04.md`
