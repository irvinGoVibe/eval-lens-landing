- 18:06 — создан project skill `debug-physical-iphone` для дебага локального сайта на реальном iPhone по Wi-Fi.
  - Разделены manual mode (пользователь управляет обычной Safari-вкладкой) и automated mode (Codex управляет изолированным Apple WebDriver window).
  - Добавлен helper `.agents/skills/debug-physical-iphone/scripts/iphone-session.mjs` с командами `doctor`, `manual`, `automate` и обязательным release по `Ctrl+C`.
  - Добавлены правила LAN IP discovery, проверки paired iPhone, безопасной смены driver port, Safari Web Inspector и обработки зависшего scroll scene.
  - `AGENTS.md` направляет задачи физического iPhone/Wi-Fi к новому skill; Safari/WebKit regression остаётся в `safari-compatibility-audit`.
  - Validation: Node syntax passed; Codex skill validator passed; doctor корректно обнаружил текущий LAN URL и paired iPhone. Сервер 3005 на момент doctor не был доступен, поэтому automation намеренно не запускалась.

- 18:55 — создан и впервые применён project skill `mobile-qa-loop`; scope первого audit-only прохода ограничен клиентской главной `/`.
  - Матрица: Chromium + Playwright WebKit, 375×812 и 430×932, обычный `prefers-reduced-motion: no-preference`, 75% viewport scroll checkpoints, modal state.
  - Изолированный production build успешен; основной сервер 3005 и занятый порт 3000 не затрагивались. Телефонный сервер доступен на `http://192.168.1.2:3405/`.
  - Результат инфраструктуры: 4/4 Playwright-прохода passed; page errors 0; failed requests 0; document-level horizontal overflow 0.
  - `HOME-MOBILE-001` (high, WebKit): при переходе из второго тёмного sticky-экрана в светлую секцию левый край `Launch your entry point` и первой карточки срезается за viewport. Chromium при тех же checkpoint/viewport отображает корректно. Вероятная зона причины — sticky/transform состояние на границе секций.
  - `HOME-MOBILE-002` (high, shared): второй scroll-scrub экран имеет чрезмерно длинную почти пустую фазу до появления закреплённого контента; на мобильном воспринимается как зависание. Связано с `height: 320svh`, фазами `HOLD_END/LIFT_START/VIDEO_START` и fixed-heading orchestration.
  - `HOME-MOBILE-003` (medium, shared): Next prefetch для `/try-live-demo?_rsc=...` получает 404 в Chromium и WebKit; CTA ведёт на отсутствующий клиентский маршрут.
  - Исключены false positives: модальное окно намеренно перекрывает фон и не считается fixed-overlap дефектом.
  - Evidence/report: `web/mobile-tests/runs/2026-07-20T11-51-08-652Z/` (локальный ignored artifact); HTML report, четыре JSON report и 70 checkpoint screenshots.
  - Physical iPhone: после переподключения CoreDevice восстановил существующую пару (`paired`, wired tunnel `connected`) без нового диалога Trust. Apple `safaridriver` успешно создал реальную iOS Safari 26.5.2 WebDriver-сессию при `developerMode: disabled`; значит этот флаг не является блокером для уже разрешённой Safari Remote Automation.
  - Physical Safari viewport: 402×660 CSS px, DPR 3, `viewport-fit=cover`; document-level horizontal overflow отсутствует (`scrollWidth === innerWidth === 402`).
  - `HOME-MOBILE-002` подтверждён на настоящем iPhone: на scroll checkpoints 1200–1700 scrub-секция теряет почти весь видимый контент и выглядит как зависший чёрный экран; перед светлой секцией на checkpoint 2750 остаётся большой пустой тёмный разрыв. Заголовок `Launch your entry point` появляется только после выхода scrub-pin и не удерживается в ожидаемой фазе перехода.
  - Physical evidence: `web/test-results/real-safari/live/home-physical-2026-07-20/` и `web/test-results/real-safari/live/home-scroll-physical-2026-07-20/` (локальные ignored artifacts, checkpoints 760/1200/1700/2200/2750/3050).
