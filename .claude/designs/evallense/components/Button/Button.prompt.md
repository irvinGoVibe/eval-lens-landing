The EvalLense pill CTA — use for every call to action. On the site the only two
labels are "Book a demo" (primary) and "Try live demo" (secondary).

```jsx
<Button variant="primary" arrow>Book a demo</Button>
<Button variant="glass">Try live demo</Button>
```

Variants: `primary` (dark Apple pill, light surfaces), `glass` (translucent
liquid-glass, for hero/dark/cinematic surfaces), `ghost` (hairline outline),
`dark` (solid ink), `gradient` (purple→blue filled, for in-app primary actions).
Sizes `md` | `sm`. `arrow` adds a trailing →. Pass `href` to render an anchor.
