---
name: redraw-block
description: "Перерисовать ОДИН существующий компонент/секцию EvalLense в Apple-style, сохраняя дизайн-систему (токены, liquid-glass, ScrollOrchestrator, серверность) и контракт компонента. Редизайн визуальный, не функциональный: пропсы/данные/API сохраняются, различия выражаются пропсами в духе sections/lab/. Один компонент за вызов. Сборку страниц из блоков делает build-pages — этот скилл готовит сами блоки. Триггеры: «перерисуй компонент», «передизайнь блок <Имя> в apple-style», «redraw <Component>», «сделай apple-style из <секция>», «приведи <блок> к дизайн-системе»."
metadata:
  version: 1.0.0
  product: EvalLense
---

# Redraw Block — редизайн компонента в Apple-style

Ты — фронтенд-дизайнер EvalLense. Берёшь **один существующий компонент**
(`web/src/components/**`) и **перерисовываешь его в Apple-style**, оставаясь
строго внутри дизайн-системы и сохраняя поведение.

Рабочий диалог — **на русском**. UI-копия в коде — на английском (язык сайта),
если не попросили иначе.

> **Главный принцип:** редизайн **визуальный, не функциональный.** Пропсы, API,
> серверность, данные и место компонента в `ScrollOrchestrator` сохраняются.
> Меняются раскладка, иерархия, ритм, воздух, движение — по грамматике
> Apple-блоков. Различия страниц/контекстов выражаются **пропсами**, а не
> форком компонента (паттерн `sections/lab/`, память «section-types-as-components»).

> **Scope:** один компонент за вызов. Сборку страниц из готовых блоков делает
> **build-pages** — этот скилл его кормит, а не заменяет.

---

## Константы (пути)

Пути даны с fallback (скилл может запускаться из корня репо или из родителя):

- **Компоненты:** `web/src/components/**` (секции — `components/sections/`,
  shared UI — `components/ui/`, стенд prop-driven секций — `components/sections/lab/`).
- **Токены и классы (источник правды по стилю):** `web/src/app/globals.css`.
- **Рейлы проекта:** `CLAUDE.md` (glass-группа, ScrollOrchestrator, серверность, pnpm, порт).
- **Каталог и ритм секций (живые):** `wiki/architecture/section-types.md`,
  `wiki/architecture/page-design-patterns.md`, `wiki/architecture/design-system.md`.
- **Оркестратор анимаций:** `web/src/components/ScrollOrchestrator.tsx` (единый rAF).

---

## Шаг 0. Загрузи живой контекст дизайн-системы (обязательно)

НЕ держи токены/рейлы в памяти — подгружай из репо каждый запуск, чтобы скилл
не устаревал.

Токены, классы, liquid-glass группа:
!`sed -n '1,200p' web/src/app/globals.css 2>/dev/null || sed -n '1,200p' eval-lens-landing/web/src/app/globals.css 2>/dev/null || echo "НЕ НАЙДЕН globals.css — проверь путь web/src/app/globals.css"`

Жёсткие рейлы проекта (glass, анимации, серверность, pnpm, порт):
!`cat CLAUDE.md 2>/dev/null || cat eval-lens-landing/CLAUDE.md 2>/dev/null || echo "НЕ НАЙДЕН CLAUDE.md"`

Грамматика Apple-блоков (авторские правила композиции этого скилла —
[references/apple-block-grammar.md](references/apple-block-grammar.md)):
!`cat .claude/skills/redraw-block/references/apple-block-grammar.md 2>/dev/null || cat eval-lens-landing/.claude/skills/redraw-block/references/apple-block-grammar.md 2>/dev/null || echo "НЕ НАЙДЕН references/apple-block-grammar.md"`

Каталог реальных типов секций (классы, поверхности — бери существующий тип, не плоди near-дубли):
!`cat wiki/architecture/section-types.md 2>/dev/null || cat eval-lens-landing/wiki/architecture/section-types.md 2>/dev/null || echo "НЕ НАЙДЕН section-types.md"`

Ритм/архетипы/«не плоско ли»/QA:
!`cat wiki/architecture/page-design-patterns.md 2>/dev/null || cat eval-lens-landing/wiki/architecture/page-design-patterns.md 2>/dev/null || echo "НЕ НАЙДЕН page-design-patterns.md"`

---

## Шаг 1. Определи цель и сними baseline компонента

**Вход — имя компонента** (`Hero`, `Problem`, `Workflow`, `Button`, …) или путь.
Прочитай его файл **целиком** и зафиксируй контракт, который НЕЛЬЗЯ ломать:

- **Пропсы / API** — сигнатура, что компонент принимает и рендерит.
- **Серверность** — Server Component или `"use client"`? (Секции — обычно Server.)
  Редизайн не должен превращать Server-секцию в клиентскую без причины.
- **Данные** — откуда контент (пропсы, `lib/blog.ts`, статика).
- **Движение** — какие `data-reveal` / `data-scrub` / `data-pin` уже есть и как
  их читает `ScrollOrchestrator`. Сохрани крючки, по которым он находит блок.
- **glass** — использует ли `<Button variant="glass">` / `.btn-glass`. Если да —
  материал берётся из общей liquid-glass группы (см. Шаг 3), не локально.
- **Токены vs хардкод** — что уже на CSS-vars, что захардкожено (кандидат на чистку).

Сформулируй **идею блока одним предложением** (грамматика, правило 1). Если идей
несколько — отметь это: возможно, блок надо разбить (но это решение user'а).

---

## Шаг 2. (Опционально) Визуальный baseline — только по просьбе

Per CLAUDE.md **не запускай dev-сервер сам.** Если нужно увидеть «как было/как
стало» — **спроси user'а**. Когда он согласится — `preview_start` (name `web`,
порт 3005, см. `.claude/launch.json`), затем `preview_screenshot`. Иначе работай
по коду без превью.

---

## Шаг 3. Спроектируй редизайн по грамматике Apple-блоков

Применяй `references/apple-block-grammar.md` к этому блоку. Реши явно:

1. **Архетип** — какой тип из `section-types.md` лучше несёт идею (statement-hero,
   pinned-multi-screen, stat-band, bento, horizontal-gallery, risk-control, quiet-cta…).
   Бери существующий тип с его классом-примером, не изобретай near-дубль.
2. **Поверхность** — `light` или `ink` под замысел страницы (правило 5).
   Чередование НЕ обязательно: несколько светлых или несколько тёмных подряд —
   норма. Проверь **стык с соседними блоками**: одинаковая поверхность → общая
   база фона + единый тип перехода (hairline / мягкое затухание / общий фон),
   чтобы шва не было видно.
3. **Иерархия типа** — eyebrow → `.title` → `.sub` → body; заголовок несёт смысл
   сам (правило 2).
4. **Визуал** — один герой-кадр или осознанный `.media-ph` (правило 4).
5. **Движение** — один тип через `data-*`, easing `--ease` (правило 6). Анимация
   живёт в `ScrollOrchestrator`, не в новом `useEffect`.
6. **Акцент** — ≤1 lens-акцент, остальное нейтральная база (правило 7).

Прогони замысел против анти-паттернов грамматики и чек-листа «не плоско ли» из
`page-design-patterns.md`. Кратко покажи план user'у перед правкой кода, если
редизайн заметный.

---

## Шаг 4. Реализуй, не нарушая рейлы

Точечные правки в файле компонента (+ при необходимости `globals.css`). Держи:

- **Токены только через CSS-vars** — никакого хардкода цветов/радиусов/теней.
  Используй существующие имена из globals.css (`--bg`/`--bg-soft`/`--ink`/`--fg`,
  `--violet`/`--cyan`/`--aqua`, `--r-*`, `--sh-*`, `--lens`, `--maxw`, `--gutter`).
- **liquid-glass — только общая группа.** Если блоку нужен glass-материал —
  добавь его селектор в общую multi-selector группу в `globals.css` (поиск
  `liquid glass` / `.btn-glass`), НЕ изобретай локальный glass-look (рейл CLAUDE.md).
- **Анимации — только в `ScrollOrchestrator.tsx`** (единый rAF). Новый эффект —
  это новый `data-*` + обработка в оркестраторе, не per-section `useEffect`.
- **Секции остаются Server Components**; `"use client"` — только если уже был или
  реально нужен (интерактив). Не превращай статику в клиент без причины.
- **Prop-driven** — если редизайн вводит вариант, делай его пропсом в духе
  `sections/lab/` (различия = пропсы), а не копией компонента.
- **pnpm only** — никаких `npm install` / `package-lock.json`.

---

## Шаг 5. Верификация

- **Сборка:** `cd web && pnpm build` — обязательно, должна пройти.
- **Визуально:** только если user попросил превью (Шаг 2) — `preview_screenshot`
  «после», сравни с baseline. Сам сервер не поднимай.

---

## Шаг 6. Отчёт в чат + чек-лист

Короткий отчёт по-русски:
1. **Что перерисовал** — какой архетип/поверхность/иерархию выбрал и почему.
2. **Что сохранил** — пропсы, серверность, данные, крючки `ScrollOrchestrator`.
3. **Рейлы** — токены/glass/анимации/pnpm соблюдены; что чистил от хардкода.
4. **Сборка** — результат `pnpm build`.
5. **Открытые вопросы** — если блок просил разбиения/новых данных/ассета.

Чек-лист сдачи:
- [ ] Идея блока — одно предложение; иерархия типа statement-first.
- [ ] Архетип из `section-types.md` (не near-дубль); поверхность light/ink под замысел страницы.
- [ ] Стык с соседними блоками бесшовный (общая база фона, единый тип перехода), а не контраст ради контраста.
- [ ] Один герой-визуал / осознанный `.media-ph`; воздух щедрый; контент в `--maxw`.
- [ ] Движение через `data-*` в `ScrollOrchestrator` (НЕ новый `useEffect`); easing `--ease`; один тип.
- [ ] ≤1 lens-акцент; цвет семантичен; только токены, без хардкода.
- [ ] glass — через общую liquid-glass группу, не локально.
- [ ] Контракт сохранён: пропсы/API/серверность/данные целы; вариативность = пропсы.
- [ ] `cd web && pnpm build` проходит.
- [ ] pnpm, без npm/package-lock; порт 3005 не менялся.

---

## Вопросы, которые задаёшь user'у (если не ясно)

1. Какой компонент перерисовать (имя/путь)? Один за вызов.
2. Есть референс/направление (конкретный Apple-паттерн, эталонная секция сайта)?
3. Нужно ли превью «до/после» (тогда подниму dev-сервер) или работать по коду?
4. Можно ли вводить новые пропсы/варианты, или сохранить сигнатуру 1:1?

## Связанные скиллы

- **build-pages** — собирает страницы из готовых блоков (downstream-потребитель).
- **evallense-site** — готовит продуктовые брифы (контент) для страниц.
- **section-lab / sections/lab/** — стенд prop-driven секций; держи редизайн в его духе.
